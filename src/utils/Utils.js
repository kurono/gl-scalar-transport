/**
 * A collection of helper functions
 */
class Utils {
    /**
     * Get the class name of an object 
     * @param {Object} object 
     * @returns {string} String represenation of the class name
     */
    static getClassName(object) {
        return object.constructor.name;
    }

    
    static log(...args) {

    }
}